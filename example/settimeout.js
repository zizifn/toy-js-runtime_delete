import { setTimeout } from "qjs:os"

// setTimeout(() => {
//     console.log("setTimeout is 1000-----------");
// }, 1000)

// Promise.resolve("xxxx").then(()=>{})

const p1 = new Promise((resolve, reject) => {
    // setTimeout(() => resolve('p1'), 1000);
});
setTimeout("xxxxx", 1000)